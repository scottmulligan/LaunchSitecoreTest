@model Sitecore.Speak.Components.Models.Common.TimePickers.TimePickerRenderingModel
@{
  Model.Class.Append("sc-timepicker time form-control");

  Model.DataBind
    .IsVisible()
    .IsEnabled()
    .Bind("attr", "{ readonly: IsReadOnly, title: Tooltip() ? Tooltip : null }");
}
<input type="text" @Model.HtmlAttributes />